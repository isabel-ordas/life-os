# context-library/
# This folder contains personal user data generated by life-os skills.
# It is intentionally excluded from version control (.gitignore).
# Each user's context-library is private to their own setup.
